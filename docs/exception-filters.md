<!-- 此文件从 content/exception-filters.md 自动生成，请勿直接修改此文件 -->
<!-- 生成时间: 2026-03-13T04:23:21.532Z -->
<!-- 源文件: content/exception-filters.md -->

### 异常过滤器

Nest 提供了一个内置的 **异常层**，负责处理应用程序中的所有未处理的异常。当应用程序代码不能处理异常时，该层将自动发送一个适合的用户友好响应。

__HTML_TAG_138__
  __HTML_TAG_139__
__HTML_TAG_140__

默认情况下，这个操作是由内置的 **全局异常过滤器** 执行的，该过滤器处理 __INLINE_CODE_18__ 类型（及其子类）的异常。对于未识别的异常（既不是 __INLINE_CODE_19__ 也不是 __INLINE_CODE_20__ 的子类），内置的异常过滤器生成以下默认 JSON 响应：

```bash
$ npm i -g @nestjs/cli
$ nest new project-name

```

> info **Hint** 全局异常过滤器部分支持 __INLINE_CODE_21__ 库。基本上，任何包含 __INLINE_CODE_22__ 和 __INLINE_CODE_23__ 属性的抛出的异常将被正确地填充并发送回响应（而不是未识别异常的默认 __INLINE_CODE_24__）。

#### 抛出标准异常

Nest 提供了一个内置的 __INLINE_CODE_25__ 类，从 __INLINE_CODE_26__ 包中导出。对于常规的 HTTP REST/GraphQL API 应用程序，最佳做法是在某些错误条件发生时发送标准 HTTP 响应对象。

例如，在 __INLINE_CODE_27__ 中，我们有一个 __INLINE_CODE_28__ 方法（一个 __INLINE_CODE_29__ 路由处理器）。让我们假设这个路由处理器抛出异常。要演示这个，我们将硬编码如下：

```bash
$ git clone https://github.com/nestjs/typescript-starter.git project
$ cd project
$ npm install
$ npm run start

```

> info **Hint** 我们使用了 __INLINE_CODE_30__。这是来自 __INLINE_CODE_31__ 包的帮助枚举。

当客户端调用这个端点时，响应如下：

__CODE_BLOCK_2__

__INLINE_CODE_32__ 构造函数需要两个必需参数来确定响应：

- __INLINE_CODE_33__ 参数确定 JSON 响应体。它可以是一个 __INLINE_CODE_34__ 或 __INLINE_CODE_35__，如以下所述。
- __INLINE_CODE_36__ 参数确定 __LINK_147__。

默认情况下，JSON 响应体包含两个属性：

- __INLINE_CODE_37__: 默认为提供在 __INLINE_CODE_38__ 参数中的 HTTP 状态代码
- __INLINE_CODE_39__: HTTP 错误的短描述，基于 __INLINE_CODE_40__

要覆盖 JSON 响应体的消息部分，提供一个字符串在 __INLINE_CODE_41__ 参数中。要覆盖整个 JSON 响应体，传递一个对象在 __INLINE_CODE_42__ 参数中。Nest 将序列化对象并将其作为 JSON 响应体返回。

第二个构造函数参数 - __INLINE_CODE_43__ - 应该是一个有效的 HTTP 状态代码。最佳实践是使用 __INLINE_CODE_44__ 枚举，来自 __INLINE_CODE_45__。

第三个构造函数参数（可选） - __INLINE_CODE_46__ - 可以用来提供一个错误 __LINK_148__。这个 __INLINE_CODE_47__ 对象不会被序列化到响应对象，但可以用于日志记录，提供有价值的信息，指明导致 __INLINE_CODE_48__ 被抛出的内错误。

以下是一个覆盖整个响应体并提供错误原因的示例：

__CODE_BLOCK_3__

使用上述内容，这是响应将看起来的样子：

__CODE_BLOCK_4__

#### 异常日志记录

默认情况下，异常过滤器不记录内置异常，如 __INLINE_CODE_49__（及其子类）。当这些异常被抛出时，它们不会出现在控制台，因为它们被视为正常应用程序流程的一部分。同样适用于其他内置异常，例如 __INLINE_CODE_50__ 和 __INLINE_CODE_51__。

这些异常都继承自基本的 __INLINE_CODE_52__ 类，从 __INLINE_CODE_53__ 包中导出。这类帮助区分异常，区分那些是正常应用程序操作的一部分，而不是。

如果你想记录这些异常，可以创建一个自定义的异常过滤器。我们将在下一节中解释如何做到这一点。

#### 自定义异常

在许多情况下，你不会需要编写自定义异常，可以使用 Nest 的内置 HTTP 异常，如下一节所述。如果你确实需要创建自定义异常，好的实践是创建自己的 **异常层次结构**，其中你的自定义异常继承自基本的 __INLINE_CODE_54__ 类。这样，Nest 就会识别你的异常，并自动处理错误响应。让我们实现这样一个自定义异常：

__CODE_BLOCK_5__

由于 __INLINE_CODE_55__ 扩展了基本的 __INLINE_CODE_56__，它将与内置的异常处理程序一起工作，因此我们可以在 __INLINE_CODE_57__ 方法中使用它。

__CODE_BLOCK_6__

#### 内置 HTTP 异常Here is the translation:

Nest 提供了一套标准的异常类，继承自基础的 __CODE_58__。这些异常类暴露在 __CODE_59__ 包中，代表了许多常见的 HTTP 异常：

- __CODE_60__
- __CODE_61__
- __CODE_62__
- __CODE_63__
- __CODE_64__
- __CODE_65__
- __CODE_66__
- __CODE_67__
- __CODE_68__
- __CODE_69__
- __CODE_70__
- __CODE_71__
- __CODE_72__
- __CODE_73__
- __CODE_74__
- __CODE_75__
- __CODE_76__
- __CODE_77__
- __CODE_78__
- __CODE_79__

所有内置的异常类都可以提供错误信息和错误描述使用 __CODE_81__ 参数：

__CODE_BLOCK_7__

使用上述信息，这是响应将看起来：

__CODE_BLOCK_8__

#### 异常过滤器

在基本（内置）异常过滤器可以自动处理许多情况时，您可能想要完全控制异常层。例如，您可能想要添加日志记录或根据一些动态因素使用不同的 JSON 模式。 **异常过滤器** 正是为此目的设计的。它们让您控制精确的控制流和客户端接收的响应内容。

让我们创建一个异常过滤器，该过滤器负责捕捉__INLINE_CODE_82__ 类型的异常，并实现自定义响应逻辑。为此，我们需要访问底层平台 __CODE_83__ 和 __CODE_84__ 对象。我们将访问 __CODE_85__ 对象，以便从中提取原始 __CODE_86__ 并将其包含在日志信息中。我们将使用 __CODE_87__ 对象来控制响应内容，使用 __CODE_88__ 方法。

__CODE_BLOCK_9__

> 提示 **Hint** 所有异常过滤器都应实现泛型 __CODE_89__ 接口。这要求您提供 __CODE_90__ 方法，并且 __CODE_91__ 表示异常的类型。

> 警告 **Warning** 如果您使用 __CODE_92__，可以使用 __CODE_93__ 而不是 __CODE_94__。请记住从 __CODE_95__ 导入正确的类型。

__CODE_96__ 装饰器将所需的元数据绑定到异常过滤器中，告诉 Nest 这个特定的过滤器正在寻找类型为 __CODE_97__ 的异常，并且什么都不做。 __CODE_98__ 装饰器可以接受单个参数或逗号分隔的列表。这使您可以设置过滤器来处理多种类型的异常。

#### Arguments host

让我们查看 __CODE_99__ 方法的参数。 __CODE_100__ 参数是当前正在处理的异常对象。 __CODE_101__ 参数是一个 __CODE_102__ 对象。 __CODE_103__ 是一个强大的utility 对象，我们将在 __LINK_149__ 中进行 further 探讨。在这个代码示例中，我们使用它来获取对 __CODE_104__ 和 __CODE_105__ 对象的引用，这些对象是在原始请求处理程序（在控制器中出现异常的地方）中被传递的。在这个代码示例中，我们使用了 __CODE_106__ 的一些helper 方法来获取所需的 __CODE_107__ 和 __CODE_108__ 对象。了解更多关于 __CODE_109__ 的信息，请访问 __LINK_150__。

*原因是 __CODE_110__ 函数在所有上下文中都可用（例如，我们现在工作的 HTTP 服务器上下文，但也包括 Microservices 和 WebSockets）。在执行上下文章节中，我们将看到我们可以使用 __CODE_111__ 和其helper 函数来访问适当的 __HTML_TAG_141__ underlying arguments__HTML_TAG_142__以便在所有上下文中编写通用的异常过滤器。

__HTML_TAG_143____HTML_TAG_144__

#### 绑定过滤器

让我们将我们的新 __CODE_112__ 绑定到 __CODE_113__ 的 __CODE_114__ 方法中。

__CODE_BLOCK_10__

> 提示 **Hint** __CODE_115__ 装饰器来自 __CODE_116__ 包。

我们使用了 __CODE_117__ 装饰器。类似于 __CODE_118__ 装饰器，它可以接受单个过滤器实例或逗号分隔的列表。在这里，我们在 place 创建了 __CODE_119__ 的实例。或者，您也可以传递类（而不是实例），将责任留给框架，并启用 **依赖注入**。

__CODE_BLOCK_11__

> 提示 **Hint** 尽量使用类来应用过滤器，而不是实例。这可以减少 **内存使用**，因为 Nest 可以轻松地重用同一个类的实例跨越整个模块。Here is the translation of the English technical documentation to Chinese:

在上面的示例中，__INLINE_CODE_120__仅应用于单个__INLINE_CODE_121__路由处理器，使其方法作用域。异常过滤器可以在不同的级别上作用：方法作用域的控制器/解决器/网关、控制器作用域或全局作用域。

例如，要将过滤器设置为控制器作用域，可以按照以下方式操作：

__CODE_BLOCK_12__

该构造将为__INLINE_CODE_123__中的每个路由处理器设置__INLINE_CODE_122__。

要创建全局作用域的过滤器，可以按照以下方式操作：

__CODE_BLOCK_13__

> warning **警告** __INLINE_CODE_124__方法不会为网关或混合应用程序设置过滤器。

全局作用域的过滤器将在整个应用程序中使用，涵盖每个控制器和每个路由处理器。在依赖注入方面，全局过滤器注册在任何模块外（如示例中使用__INLINE_CODE_125__）不能注入依赖项，因为这是在任何模块的上下文外完成的。要解决这个问题，可以在任何模块中直接注册全局过滤器，使用以下构造：

__CODE_BLOCK_14__

> info **提示** 使用该方法在过滤器中执行依赖注入时，请注意，无论是在哪个模块中使用该构造，该过滤器实际上是全局的。应该在哪个模块中使用该构造？选择定义过滤器（如示例中的__INLINE_CODE_126__）的模块。也可以使用__INLINE_CODE_127__来处理自定义提供者注册。了解更多__LINK_151__。

可以添加任意多个过滤器使用该技术，只需将每个过滤器添加到 providers 数组中。

#### 捕捉一切

要捕捉**任何**未处理的异常（无论是哪种异常类型），请将__INLINE_CODE_128__装饰器的参数列表留空，例如__INLINE_CODE_129__。

在以下示例中，我们有一个平台无关的代码，因为它使用__LINK_152__来传递响应，并且不直接使用平台特定的对象(__INLINE_CODE_130__和__INLINE_CODE_131__)：

__CODE_BLOCK_15__

> warning **警告** 将“捕捉一切”过滤器与绑定到特定类型的过滤器结合时，“Catch anything”过滤器应该在前，以允许具体过滤器正确地处理绑定的类型。

#### 继承

通常，您将创建完全自定义的异常过滤器，以满足应用程序要求。然而，有些情况下，您可能想简单地扩展内置的默认**全局异常过滤器**，并根据某些因素Override行为。

要委派异常处理到基础过滤器，需要扩展__INLINE_CODE_132__并调用继承的__INLINE_CODE_133__方法。

__CODE_BLOCK_16__

> warning **警告** 方法作用域和控制器作用域的过滤器，扩展__INLINE_CODE_134__时，不应该使用__INLINE_CODE_135__。相反，让框架自动实例化它们。

全局过滤器**可以**扩展基础过滤器。这可以通过两种方法完成。

第一种方法是在自定义全局过滤器中注入__INLINE_CODE_136__引用：

__CODE_BLOCK_17__

第二种方法是使用__INLINE_CODE_137__令牌__HTML_TAG_145__，如下所示__HTML_TAG_146__。