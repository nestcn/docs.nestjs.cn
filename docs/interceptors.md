<!-- 此文件从 content/interceptors.md 自动生成，请勿直接修改此文件 -->
<!-- 生成时间: 2026-03-17T04:31:57.184Z -->
<!-- 源文件: content/interceptors.md -->

### 拦截器

拦截器是一个带有 __INLINE_CODE_12__ 装饰器的类，实现了 __INLINE_CODE_13__ 接口。

__HTML_TAG_86____HTML_TAG_87____HTML_TAG_88__

拦截器具有以下有用的功能，这些功能受到__LINK_91__（面向方面编程）技术的灵感。它们使得：

- 在方法执行前/后绑定额外的逻辑
- تحويل返回的函数结果
- 转换抛出函数的异常
- 延伸基本函数行为
- 根据特定条件完全覆盖函数

#### 基本信息

每个拦截器都实现了 __INLINE_CODE_14__ 方法，该方法接受两个参数。第一个参数是 __INLINE_CODE_15__ 实例（与 __LINK_92__ 中的实例相同），__INLINE_CODE_16__ 继承自 __INLINE_CODE_17__。我们在异常过滤器章节中见过 __INLINE_CODE_18__，它是对原始处理程序传递的参数的包装器，包含根据应用程序类型的不同参数数组。您可以在 __LINK_93__ 中了解更多信息。

#### 执行上下文

通过继承 __INLINE_CODE_19__，__INLINE_CODE_20__ 还添加了一些新的helper方法，它们提供了关于当前执行过程的更多信息。这些信息可以帮助您构建可以在广泛的控制器、方法和执行上下文中工作的更通用的拦截器。了解更多关于 __INLINE_CODE_21__ 的信息请查看 __LINK_94__。

#### 调用处理程序

第二个参数是 __INLINE_CODE_22__。__INLINE_CODE_23__ 接口实现了 __INLINE_CODE_24__ 方法，您可以使用该方法来在拦截器中调用路由处理程序方法。如果您在 __INLINE_CODE_26__ 方法的实现中不调用 __INLINE_CODE_25__ 方法，路由处理程序方法将不会被执行。

这种方法意味着 __INLINE_CODE_27__ 方法实际上将请求/响应流包围。在结果中，您可以在 __INLINE_CODE_28__ 方法中编写代码执行在路由处理程序之前或之后。正如您可以在 __INLINE_CODE_28__ 方法中编写在路由处理程序之前执行的代码一样，您也可以影响在路由处理程序之后发生的事情？因为 __INLINE_CODE_30__ 方法返回一个 __INLINE_CODE_31__，我们可以使用强大的 __LINK_95__ 运算符来进一步处理响应。使用面向方面编程术语，路由处理程序的调用（即调用 __INLINE_CODE_32__）被称为__LINK_96__，表示在其中插入额外逻辑的地方。

#### 方面拦截

我们将首先查看使用拦截器来记录用户交互（例如，存储用户调用、异步派发事件或计算时间戳）的用例。我展示了一个简单的 __INLINE_CODE_42__：

```bash
$ npm i -D @compodoc/compodoc

```

> info **提示** __INLINE_CODE_43__ 是一个通用的接口，其中 __INLINE_CODE_44__ 表示__INLINE_CODE_45__ 类型的值（支持响应流），并且 __INLINE_CODE_46__ 是__INLINE_CODE_47__ 包装的值类型。

> warning **注意** 拦截器，像控制器、提供者、守卫和其他类似的对象，可以通过它们的 __INLINE_CODE_48__ 注入依赖项。

由于 __INLINE_CODE_49__ 返回一个 RxJS __INLINE_CODE_50__，我们有很多选择可以使用来操作流。在上面的示例中，我们使用了 __INLINE_CODE_51__ 运算符，该运算符在观察流的优雅或异常终止时调用我们的匿名记录函数，但不会干扰响应周期。

#### 绑定拦截器

要设置拦截器，我们使用来自 __INLINE_CODE_53__ 包的 __INLINE_CODE_52__ 装饰器。像 __LINK_97__ 和 __LINK_98__ 一样，拦截器可以是控制器作用域、方法作用域或全局作用域。

```bash
$ npx @compodoc/compodoc -p tsconfig.json -s

```

> info **提示** __INLINE_CODE_54__ 装饰器来自 __INLINE_CODE_55__ 包。

使用上面的构建，每个路由处理程序定义在 __INLINE_CODE_56__ 中都会使用 __INLINE_CODE_57__。当 Someone 调用 __INLINE_CODE_58__ 端口时，您将在标准输出中看到以下输出：

__CODE_BLOCK_2__Here is the translation of the provided English technical documentation to Chinese:

注意，我们将 __INLINE_CODE_59__ 类（而不是实例）传递给框架，这样可以让框架负责实例化并启用依赖注入。像管道、守卫和异常过滤器一样，我们也可以传递一个在-place 实例：

__CODE_BLOCK_3__

正如之前所提到的，构建中的拦截器将被_attach 到这个控制器中声明的每个处理程序上。如果我们想将拦截器的作用域限制到单个方法，可以将装饰器应用于 **方法级别**。

要设置全局拦截器，我们使用 Nest 应用程序实例的 __INLINE_CODE_60__ 方法：

__CODE_BLOCK_4__

全局拦截器将在整个应用程序中使用，每个控制器和路由处理程序都将使用它。在依赖注入方面，注册于任何模块外部的全局拦截器（使用 __INLINE_CODE_61__，如上面的示例）不能注入依赖项，因为这是在任何模块外部进行的。要解决这个问题，可以在模块中使用以下构造来设置拦截器：

__CODE_BLOCK_5__

> 提示 **Hint** 使用这个方法来为拦截器进行依赖注入时，请注意，不管是在哪个模块中使用这个构造，拦截器实际上是全局的。应该在哪个模块中使用这个构造？选择定义拦截器的模块（例如上面的 __INLINE_CODE_62__）。此外， __INLINE_CODE_63__ 并不是唯一的自定义提供者注册方式。了解更多 __LINK_99__。

#### 响应映射

我们已经知道 __INLINE_CODE_64__ 返回的是 __INLINE_CODE_65__。流包含了路由处理程序返回的值，因此我们可以使用 RxJS 的 __INLINE_CODE_66__ 运算符轻松地对其进行 mutate。

> 警告 **Warning** 响应映射特性不支持库专门的响应策略（使用 __INLINE_CODE_67__ 对象直接是被禁止的）。

让我们创建一个 __INLINE_CODE_68__，它将在一个简单的方式中修改每个响应，以 demonstrate这个过程。它将使用 RxJS 的 __INLINE_CODE_69__ 运算符将响应对象赋值给 __INLINE_CODE_70__ 属性的新创建对象，并将新对象返回给客户端。

__CODE_BLOCK_6__

> 提示 **Hint** Nest 拦截器可以与同步和异步 __INLINE_CODE_71__ 方法一起工作。您可以简单地将方法 switch 到 __INLINE_CODE_72__ 如果必要。

使用上面的构造，当 someone 调用 __INLINE_CODE_73__ 端口时，响应将看起来像以下所示（假设路由处理程序返回一个空数组 __INLINE_CODE_74__）：

__CODE_BLOCK_7__

拦截器在创建可重用的解决方案来满足整个应用程序中的需求中非常有价值。例如，假设我们需要将每个 __INLINE_CODE_75__ 值转换为一个空字符串 __INLINE_CODE_76__。我们可以使用一行代码并将拦截器绑定到全局，以便自动使用每个注册的处理程序。

__CODE_BLOCK_8__

#### 异常映射

另一个有趣的用例是使用 RxJS 的 __INLINE_CODE_77__ 运算符来override 异常：

__CODE_BLOCK_9__

#### 流重写

有时候，我们可能需要完全阻止调用处理程序并返回一个不同的值。一个明显的示例是实现缓存以提高响应速度。让我们来看看一个简单的 **缓存拦截器**，它将返回缓存的响应。在实际示例中，我们将需要考虑其他因素，如 TTL、缓存无效、缓存大小等，但是这超出了讨论的范围。在这里，我们将提供一个基本示例，展示主要概念。

__CODE_BLOCK_10__

我们的 __INLINE_CODE_78__ 有一个硬编码的 __INLINE_CODE_79__ 变量和一个硬编码的响应 __INLINE_CODE_80__。需要注意的是，我们返回一个新的流，这是使用 RxJS 的 __INLINE_CODE_81__ 运算符创建的，因此处理程序 **不会被调用**。当 someone 调用一个使用 __INLINE_CODE_82__ 的端口时，响应（一个硬编码的空数组）将被返回立即。在创建一个通用的解决方案时，可以使用 __INLINE_CODE_83__ 并创建一个自定义装饰器。 __INLINE_CODE_84__ 在 __LINK_100__ 章节中有详细描述。

#### 更多运算符

使用 RxJS 运算符来操纵流提供了许多能力。让我们考虑另一个常见的用例。假设你想处理 **超时** 路由请求。当你的端口不返回任何内容超过某个时间，你想终止并抛出错误响应。以下构造使得这个可能：

__CODE_BLOCK_11__

在 5 秒后，请求处理将被取消。你也可以添加自定义逻辑在抛出 __INLINE_CODE_85__ 之前（例如释放资源）。