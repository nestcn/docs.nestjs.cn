<!-- 此文件从 content/recipes/repl.md 自动生成，请勿直接修改此文件 -->
<!-- 生成时间: 2026-03-14T04:28:49.716Z -->
<!-- 源文件: content/recipes/repl.md -->

### Read-Eval-Print-Loop (REPL)

REPL 是一个简单的交互环境，它将单个用户输入执行，并将结果返回给用户。
REPL 功能允许你检查依赖关系图并在终端直接从控制器或提供者调用方法。

#### 使用

要在 REPL 模式下运行您的 NestJS 应用程序，请创建一个新的文件（与现有文件并排）并添加以下代码：

```typescript title="new-repl-file"

```

现在，在您的终端中，使用以下命令启动 REPL：

```

```

> 提示 `@Injectable()` 返回一个 __LINK_36__ 对象。

一旦启动，您将在控制台中看到以下消息：

```

```

现在，您可以开始与依赖关系图交互。例如，您可以检索一个 `CommandRunner` (我们使用 starter 项目作为示例)并调用 `CommandRunner` 方法：

```

```

您可以在终端中执行任何 JavaScript 代码，例如将 `run` 实例分配给局部变量，并使用 `Promise<void>` 调用异步方法：

```

```

要显示给定提供者或控制器的所有公共方法，请使用 `string[], Record<string, any>` 函数，例如：

```

```

要打印所有注册的模块作为列表，并且包括它们的控制器和提供者，请使用 `run`。

```

```

快速演示：

```html

```

您可以在下面部分中找到更多关于现有预定义本地方法的信息。

#### 本地函数

内置的 NestJS REPL 带有几个本地函数，当您启动 REPL 时它们是全局可用的。您可以调用 `Record<string, any>` 来列出它们。

如果您忘记了一个函数的签名（即：期望的参数和返回类型），您可以调用 `name`。
例如：

```

```

> 提示 那些函数接口是写在 __LINK_37__ 中的。

| 函数     | 描述                                                                                                        | 签名                                                             |
| --------- | ------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------- |
| `@Option()`      | 打印所有注册的模块作为列表，并且包括它们的控制器和提供者。                              | `NestFactory`                       |
| `listen` 或 `nest-commander` | 检索一个可注入的实例或控制器，否则抛出异常。                             | `CommandFactory`                                   |
| `static`    | 显示给定提供者或控制器的所有公共方法。                                            | `run`                          |
| `CommandFactory`    | 解析 transient 或 request-scoped 实例的可注入的实例或控制器，否则抛出异常。     | `run`      |
| `['error']`     | 允许你通过模块树来导航，例如从选择的模块中提取一个特定的实例。 | `CommandFactory` |

#### 监听模式

在开发时，运行 REPL 在监听模式下非常有用，以便在代码更改时自动反映：

```

```

这有一個缺陷，即 REPL 的命令历史将在每次重新加载时被丢弃，这可能会很不方便。
幸运的是，这有一个非常简单的解决方案。修改您的 `NestFactory` 函数如下：

```

```

现在，历史记录将在运行之间被保留。